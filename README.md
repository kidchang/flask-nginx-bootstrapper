# flask-nginx-bootstrapper

Create an `app` directory and place your flask code under it. Then run `docker-compose up`.
In order for your flask app to use Mongo DB, you should config your flask app to use environment variable `MONGO_HOST`.
Follow the example `flask-config-example.py` for your flask config file.
